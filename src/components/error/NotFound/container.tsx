import { NotFoundPresenter } from ".";
import { Layout } from "../../layout/Layout/container";

export const NotFound: React.FC = () => {
  return (
    <Layout>
      <NotFoundPresenter />
    </Layout>
  );
};
